name = "pi-web-sync-relay"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# Production deployment requires Durable Objects for WebSocket state sharing.
# For local dev, use: npm run dev (runs relay-server.ts with tsx)
# See: relay-server.ts for the local dev version.
#
# [[durable_objects.bindings]]
# name = "SESSION"
# class_name = "SessionDO"

[observability]
enabled = true