import { stub } from "../../_ptk/stub";
import { Chat } from "./components/Chat";

/** Root app: extracts session ID from URL, connects to relay, renders Chat. */
export default function App() {
  return stub("webapp.App");
}